/**
 * @module empty-module
 *
 * Stub module used as a Webpack/Turbopack alias for `canvas`, which is not
 * needed at runtime but is referenced by pdfjs-dist.
 */

const emptyModule = null
export default emptyModule
